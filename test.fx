fn add(x: int, y: int) -> int => x + y;

// fn hypot(x: int, y: int) -> int => +"4.5"*x + y*y - 3 + 3;
// fn transform_int(x: int, f: fn(int) -> int) -> int => hypot(3, 4)(x)[3.4];

fn main() -> void {
  print "Hello, World!";
  // print hypot(3, 4) + add(true, 4); 
}