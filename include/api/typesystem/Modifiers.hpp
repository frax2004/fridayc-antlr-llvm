#pragma once

#include "core/Common.hpp"

namespace friday::inline api::inline typesystem {
  /// @brief Modifiers
  enum Modifiers : u32 {
    CONST = 1,
  }; // enum Modifiers
} // namespace friday::api::typesystem